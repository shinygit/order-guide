describe('filter behavior', () => {
  beforeEach(() => {
    cy.login()
    cy.visit('/')
  })

  it('locks order', () => {})

  it('hides 0 count items', () => {})

  it('search works', () => {})

  it('filters by supplier', () => {})

  it('filters by location', () => {})

  it('', () => {})
})
